# Tells npm to use legacy peer dependency resolution globally for this project.
# Required because react-scripts@5.0.1 only accepts typescript@^3||^4,
# while some transitive deps may pull in typescript@5+.
legacy-peer-deps=true
